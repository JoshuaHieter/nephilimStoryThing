# Normalize line endings to LF in the repo across Windows and Linux.
# Critical: the Quartz CLI scripts (quartz/bootstrap-*.mjs) have a #! shebang
# that MUST stay LF, or Cloudflare's Linux builder fails to run them.
* text=auto eol=lf
